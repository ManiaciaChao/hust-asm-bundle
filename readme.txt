packed by maniacata with ♥

执行 launch_dosbox.bat 就可以。如果闪退就执行 点我启动.vbs

配置了环境变量和DOSBox的分辨率。

设置了几条alias方便linux选手

* ls = dir
* rm = del
* clear = cls
* cat = type

还有个zlc学长写的一键 build 脚本，执行 make 可以看到 usage。