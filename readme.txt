packed by maniacata with ♥

repo url: https://github.com/ManiaciaChao/hust-asm-bundle

执行launch.bat就可以。

配置了环境变量和DOSBox的分辨率。如果需要自行修改分辨率，请修改dosbox/dosbox.conf文件中 windowresolution=1920x1080这一行（比如改成1440x900)。

设置了几条alias方便linux选手

* ls = dir
* rm = del
* clear = cls
* cat = type

还有个zlc学长写的一键build脚本，执行make可以看到usage。